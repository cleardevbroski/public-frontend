# Render backend service URL. Both forms are accepted, but use the service URL
# without /api so requests resolve as /api/auth/send-otp, not /api/api/....
VITE_API_URL=https://your-backend.onrender.com
