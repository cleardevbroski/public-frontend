import type { Property, RentDetails } from "@/components/acres/mock-data";
export const initialRentDetails = (): RentDetails => ({ rentalPropertyType: "Apartment", configuration: "", monthlyRent: 0, securityDeposit: 0, maintenanceMode: "Included", maintenanceAmount: 0, availableFrom: "", lockInPeriod: "", preferredTenantTypes: [], superArea: "", carpetArea: "", bedrooms: 0, bathrooms: 0, floor: "", facing: "", parking: "", furnishing: "Unfurnished", petFriendly: false, nonVegAllowed: false, contactType: "Owner" });
export function validateRentDraft(p: Partial<Property>) { const d = p.rentDetails; const e: Record<string,string> = {}; if (!d?.configuration) e.configuration="Configuration is required."; if (!d?.monthlyRent) e.monthlyRent="Monthly rent is required."; if (d?.maintenanceMode === "Extra" && !d.maintenanceAmount) e.maintenanceAmount="Maintenance amount is required."; if (!d?.availableFrom) e.availableFrom="Available-from date is required."; return e; }
export function prepareRentPayload<T extends Partial<Property>>(p:T):T { const d=p.rentDetails; if(p.propertyType!=="Rent"||!d)return p; return {...p,listingType:"For Rent",configs:[d.configuration],price:`₹${d.monthlyRent.toLocaleString("en-IN")}/month`,area:d.superArea||d.carpetArea||"",badges:[...(p.badges||[]).filter(x=>x!=="Zero Brokerage"),...(d.contactType==="Owner"?["Zero Brokerage"]:[])],possession:`Available from ${d.availableFrom}`} as T; }
